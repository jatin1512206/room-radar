rules_version = '2';

// Local emulator only — allows re-seeding. Deploy firestore.rules to production.
service cloud.firestore {
  match /databases/{database}/documents {
    match /properties/{propertyId} {
      allow read, write: if true;
    }
  }
}
